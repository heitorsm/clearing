---
description: Status-driven triage: classify, tag, connect, and file every note with status inbox, plus unprocessed Clippings (obsidian-parking-lot OP2)
---

Run the triage operation (OP2) of the obsidian-parking-lot skill. Build the queue in two passes: primary search for every note with status inbox anywhere in the vault (Inbox/ captures and status-tagged clippings alike), plus a backfill of Clippings/ notes that have no status field yet (cap at 10 per session and say so). For each note, read it and propose classification (projects, insights, references, explore), tags from the existing vocabulary, connections, and a next step. Present proposals for validation, then apply per note type via the Obsidian CLI: idea notes get the triaged template and move to their semantic folder preserving wikilinks; clippings are triaged in place (status, thematic tags, connections), never overwriting Web Clipper fields and never moving, with the key question: does this clipping spawn a derived note with the user's own insight? Write the triage log to System/. If the queue has 6 or more notes and sub-agents are available, parallelize the analysis and consolidate into a single review.
